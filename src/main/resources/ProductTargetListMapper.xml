<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- TargetMapper.xml -->
<mapper namespace="com.example.ProductTargetListMapper">
<!--    <insert id="insertIntoTarget" parameterType="com.itheima.pojo.Product">-->
    <insert id="saveProduct" parameterType="java.util.List">
        INSERT INTO product_spu_sku (sku, spu,title_cn, product_category_id, end_time) VALUES
        <foreach collection="list" item="item" separator="," >
            (#{item.sku},
            #{item.spu},
            #{item.title_cn},
            #{item.product_category_id},
            <if test="item.end_time != null or item.end_time != ''" >
                #{item.end_time,jdbcType=TIMESTAMP}
            </if>
            )
        </foreach>
    </insert>

    <!--存储图片信息-->
    <insert id="savePicture" parameterType="java.util.List">
        INSERT INTO product_image (sku, image, image_index) VALUES
        <foreach collection="list" item="item" separator="," >
            (
            #{item.sku},
            #{item.image},
            #{item.image_index}
            )
        </foreach>
    </insert>


    <!--存储sku违禁信息 -->
    <insert id="saveForbidden" parameterType="java.util.List">
        INSERT INTO imdb.syn_yibai_prod_forbidden_grade (
        id,
        sku,
        platform_code,
        risk_grade,
        risk_grade_type,
        create_time,
        create_user,
        update_user,
        update_time,
        is_del,
        reason,
        forbidden_source,
        forbidden_data_type

        ) VALUES
        <foreach collection="list" item="item" separator="," >
            (
            #{item.id},
            #{item.sku},
            #{item.platform_code},
            #{item.risk_grade},
            #{item.risk_grade_type},
            #{item.create_time},
            #{item.create_user},
            #{item.update_user},
            #{item.update_time},
            #{item.is_del},
            #{item.reason},
            #{item.forbidden_source},
            #{item.forbidden_data_type}
            )
        </foreach>
    </insert>


    <!--存储sku侵权信息 -->
    <insert id="saveInfCountry" parameterType="java.util.List">
        INSERT INTO imdb.syn_yibai_prod_inf_country_grade(
        id,
        sku,
        country_code,
        risk_grade,
        risk_grade_type,
        create_time,
        create_user,
        update_user,
        update_time,
        is_del,
        infringement_type,
        infringement,
        infringement_source,
        infringement_info
        ) VALUES
        <foreach collection="list" item="item" separator="," >
            (
            #{item.id},
            #{item.sku},
            #{item.country_code},
            #{item.risk_grade},
            #{item.risk_grade_type},
            #{item.create_time},
            #{item.create_user},
            #{item.update_user},
            #{item.update_time},
            #{item.is_del},
            #{item.infringement_type},
            #{item.infringement},
            #{item.infringement_source},
            #{item.infringement_info}
            )
        </foreach>
    </insert>






</mapper>




